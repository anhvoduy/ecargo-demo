var config = {
    host            : 'localhost',
    user            : 'root',
    password        : 'P@ssw0rd',
    database        : 'ecargo_demo',
    connectionLimit : 10,
};

module.exports = config;